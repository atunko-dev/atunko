# yaml-language-server: $schema=https://raw.githubusercontent.com/reqstool/reqstool-client/main/src/reqstool/resources/schemas/v1/software_verification_cases.schema.json
filters:
  atunko:
    custom:
      includes: ids == /SVC_WEB_.*/

# No additional SVCs defined at the subproject level.
# All SVC_WEB_* cases are defined in the system-level docs/reqstool/software_verification_cases.yml
# and included here via the filter above.
cases: []
